<resources>

    <!-- Base application theme. -->
    <style name="Theme.App" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorControlNormal">@color/colorControlNormal</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="Theme.App.SongApply">
        <item name="fabDrawable">@drawable/ic_send</item>
    </style>

    <style name="Theme.App.SongList">
        <item name="fabDrawable">@drawable/ic_add</item>
    </style>

    <style name="Theme.App.Offbase">
        <item name="fabDrawable">@drawable/ic_add</item>
    </style>

    <style name="Theme.App.LostFound">
        <item name="fabDrawable">@drawable/ic_create</item>
    </style>

    <style name="Theme.App.Offbase.Detail">
        <item name="fabDrawable">@drawable/ic_create</item>
    </style>

    <style name="ThemeOverlay.App.AppBar" parent="ThemeOverlay.MaterialComponents.ActionBar">
        <item name="colorControlNormal">?colorPrimary</item>
        <item name="android:windowBackground">@color/windowBackgroundColor</item>
    </style>

    <style name="ThemeOverlay.App.Dark.ActionBar" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar">
        <item name="colorControlNormal">@android:color/white</item>
    </style>

    <style name="Theme.App.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.App.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="TextAppearance.App.Title" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:fontFamily">@font/nanum_square_regular</item>
    </style>

    <style name="TextAppearance.App.Title.Expanded" parent="TextAppearance.Design.CollapsingToolbar.Expanded">
        <item name="android:fontFamily">@font/nanum_square_regular</item>
    </style>

</resources>
